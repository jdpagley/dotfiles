Intstallation:

    cd ~
    git clone git://github.com/jdpagley/dotfiles

Create symlinks

  ln -s ~/dotfiles/.vimrc ~/.vimrc
  ln -s ~/dotfiles/.vim ~/.vim
  ln -s ~/dotfiles/.gitconfig ~/.gitconfig
  ln -s ~/dotfiles/.gitignore ~/.gitignore
  ln -s ~/dotfiles/.bash_profile ~/.bash_profile

